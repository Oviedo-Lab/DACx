% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DACx.R
\name{set.network.structure}
\alias{set.network.structure}
\title{Set network structure}
\usage{
set.network.structure(
  network,
  neuron_types = c("principal"),
  layer_names = c("layer"),
  n_layers = 1,
  n_columns = 1,
  patch_depth = 1,
  layer_height = 250,
  column_diameter = 130,
  layer_separation_factor = 3,
  column_separation_factor = 3.5,
  patch_separation_factor = 3.5,
  neurons_per_node = 30,
  recurrence_factors = 0.5,
  synaptic_neighborhood = 10
)
}
\arguments{
\item{network}{Network object to configure.}

\item{neuron_types}{Character vector giving types of neurons in the network, e.g. c("principal", "interneuron").}

\item{layer_names}{Character vector giving names of layers in the network, e.g. c("L2/3", "L4", "L5", "L6").}

\item{n_layers}{Integer giving number of layers in the network.}

\item{n_columns}{Integer giving number of columns in the network.}

\item{patch_depth}{Integer giving the number of "patches" (n_layers x n_columns sheets) in the network.}

\item{layer_height}{Numeric giving height of each layer (in units specified at network creation, default unit is microns, default value is 250.0).}

\item{column_diameter}{Numeric giving diameter of each column (in units specified at network creation, default unit is microns, default value is 130.0).}

\item{layer_separation_factor}{Numeric giving mean distance between layers as a fraction of layer height (default: 3.0).}

\item{column_separation_factor}{Numeric giving mean distance between columns as a fraction of column diameter (default: 3.5).}

\item{patch_separation_factor}{Numeric giving mean distance between network patches as a fraction of column diameter (default: 3.5).}

\item{neurons_per_node}{Matrix giving number of neurons of each type per node in each layer; dimensions must match n_layers (rows) and length of neuron_types (columns).}

\item{recurrence_factors}{List of matrices giving local recurrence factors for each layer; each matrix must have dimensions matching length of neuron_types (rows and columns).}

\item{synaptic_neighborhood}{Numeric giving the radius (in microns) within which an axon node will trigger a synapse when near a dendrite node (default: 10.0).}

\item{neuron_type_valences}{Numeric vector giving valences of each neuron type, e.g. c(1, -1) for excitatory and inhibitory neurons.}

\item{neuron_type_temporal_modulation}{Numeric matrix giving temporal modulation time components (for modulation time in the unit_time of the network) for each neuron type: bias, step size, and count cutoff (rows as neuron types, columns as components). Will example a single value or a vector of length three.}
}
\value{
The updated network object with the specified structure and local nodes generated.
}
\description{
This function sets the structure of a network object, defining its layers, columns, neuron types, and local connectivity parameters. It also generates local nodes based on the specified structure.
}
